spring.application.name=WigellkoncernenCarRental

server.port=8080

# Konfiguration för MySQL-databas
spring.datasource.url=jdbc:mysql://db:3306/rental?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=${MYSQL_ROOT_PASSWORD}

# Hibernate / JPA konfiguration
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.defer-datasource-initialization=true
spring.jpa.generate-ddl=true
spring.jpa.show-sql=true

# H2-konsolen är inte längre relevant när MySQL används
spring.h2.console.enabled=false
